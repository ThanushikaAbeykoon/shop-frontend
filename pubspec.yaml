name: sports
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  freezed_annotation: ^2.3.1
  json_annotation: 4.9.0
  http: ^0.13.4
  flutter_riverpod: ^1.0.3
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.6
  freezed: ^2.3.2
  json_serializable: ^6.7.0
  flutter_lints: ^4.0.0
  analyzer: 6.7.0
  dart_style: ^2.2.4

flutter:
  uses-material-design: true
